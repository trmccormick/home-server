# ============================================================
# Common environment variables shared across all containers
# ============================================================
# Copy to env/common.env and fill in your values:
#   cp env/common.env.example env/common.env

# Your local timezone
TZ=America/New_York

# UID/GID of the user running the containers (run: id)
PUID=1000
PGID=1000
